import { useNavigate, useLocation } from "react-router-dom";
import { Box, VStack, SimpleGrid, Card, CardBody, Image, Text, CardFooter, ButtonGroup, Input, Stack, Button, Center } from '@chakra-ui/react';

const Navbar = () => {
  return (
    <Center bg='black' w='1000px' h='100px' color='white'>
      <Box>
        Dancypants
      </Box>
    </Center>
  );
};

export default Navbar;